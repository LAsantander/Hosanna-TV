<resources>
    <!-- Volvemos al tema compatible, pero forzando fondo negro para la Smart TV -->
    <style name="Theme.CanalHosanna" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:colorBackground">@android:color/black</item>
    </style>
</resources>